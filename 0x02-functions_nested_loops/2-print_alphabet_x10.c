#include "main.h"
/**
 * print_alphabet_x10 - Prints the alphabet 10 times.
 * Return: Always 0 (success)
 */
void print_alphabet_x10(void)
{
	char i;
	int j;
	while (j < 10)
	{
		for(i = 'a'; i <= 'a';i++)
		{
			_putchar(i);
		}
		_putchar('\n');
		j++;
	}
}
